# Project: svg-mb-control

`AGENTS.md` is the canonical agent contract for this repo. Follow it first for
build workflow, repo boundary, documentation maintenance, and navigation.

## Claude Notes

- Prefer `README.md` and the current reference docs named in `AGENTS.md` over
  historical discovery notes.
- Keep answers and generated docs grounded in implemented behavior, observable
  CLI/config/test/runtime evidence, or clearly labeled recommendations.
- Do not use vague adjectives without a field name, number, or testable
  definition.
- Do not use undefined technical terms.
- Use `must` only for enforced rules, `should` only for advisory rules, and
  `is` only for current implemented behavior.
- Remove forward-looking claims unless the behavior already exists in this repo.
