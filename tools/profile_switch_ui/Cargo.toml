[package]
name = "svg-mb-profile-ui"
version = "0.1.0"
edition = "2021"
description = "Minimal local profile switch UI for SVG-MB-Control"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
